# Copy to .env for local development. Never commit the real file.
# In production set this in your host's environment settings instead
# (Vercel: Project Settings > Environment Variables).
ANTHROPIC_API_KEY=

# Optional. Defaults to claude-opus-5.
# Switch to claude-sonnet-5 or claude-haiku-4-5 to trade quality for cost.
ASK_MODEL=
